apiVersion: v1
kind: Service
metadata:
  name: {{ include "samarkand.fullname" . }}
  labels:
    {{- include "samarkand.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "samarkand.selectorLabels" . | nindent 4 }}
