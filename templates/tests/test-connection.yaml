apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "samarkand.fullname" . }}-test-connection"
  labels:
    {{- include "samarkand.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "samarkand.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
